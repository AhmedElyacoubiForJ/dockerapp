# 🔄 So nutzt man das Template
# 1. JAVA_BASE=eclipse-temurin docker compose -f ./compose/docker-compose-template.yml up
# 2. JAVA_BASE=openjdk-slim docker compose -f ./compose/docker-compose-template.yml up

#services:
#  app:
#    image: myimage:eclipse-temurin
#    container_name: mycontainer-eclipse-temurin
#    ports:
#      - "8080:8080"
#    depends_on:
#      - db
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
#      SPRING_DATASOURCE_USERNAME: ${DB_USER}
#      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
#    healthcheck:
#      test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
#      interval: 30s
#      timeout: 5s
#      retries: 3
#      start_period: 10s
#  db:
#    image: postgres:16
#    container_name: db_container
#    environment:
#      POSTGRES_DB: ${DB_APP}
#      POSTGRES_USER: ${DB_USER}
#      POSTGRES_PASSWORD: ${DB_PASSWORD}
#    ports:
#      - "5432:5432"
#    volumes:
#      - pgdata:/var/lib/postgresql/data
#
#volumes:
#  pgdata:



